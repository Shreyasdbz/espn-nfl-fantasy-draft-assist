# Fourth Down ESPN Watcher

Load this directory as an unpacked Chrome extension. It observes only rendered
ESPN fantasy-football draft pages and sends normalized, read-only snapshots to
the Fourth Down engine on `127.0.0.1:4317`. It never clicks, drafts, queues, or
modifies an ESPN page.
